{
  "name": "slidev-theme-scholarly",
  "version": "0.3.2",
  "type": "module",
  "description": "A scholarly presentation theme for Slidev with LaTeX Beamer-style styling",
  "author": "Jiaxin Peng",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jxpeng98/slidev-theme-scholarly.git"
  },
  "bugs": {
    "url": "https://github.com/jxpeng98/slidev-theme-scholarly/issues"
  },
  "homepage": "https://github.com/jxpeng98/slidev-theme-scholarly#readme",
  "keywords": [
    "slidev-theme",
    "slidev",
    "scholarly",
    "academic",
    "presentation",
    "beamer",
    "latex"
  ],
  "files": [
    "components",
    "layouts",
    "styles",
    "setup",
    "utils",
    "vite.config.ts",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./index.js"
    }
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "slidev example.md --open",
    "build": "slidev build example.md",
    "export": "slidev export example.md",
    "screenshot": "slidev export example.md --format png",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs",
    "version:print": "bun -p \"require('./package.json').version\"",
    "version:sync": "bun -e \"const fs=require('fs');const path=require('path');const pkg=require('./package.json');const extPath=path.join('vscode-extension','package.json');const ext=JSON.parse(fs.readFileSync(extPath,'utf8'));ext.version=pkg.version;fs.writeFileSync(extPath,JSON.stringify(ext,null,2)+'\\n');const docsPath=path.join('docs','package.json');const docs=JSON.parse(fs.readFileSync(docsPath,'utf8'));docs.version=pkg.version;fs.writeFileSync(docsPath,JSON.stringify(docs,null,4)+'\\n');\"",
    "bump:patch": "bun pm version patch --no-git-tag-version && bun run version:sync",
    "bump:minor": "bun pm version minor --no-git-tag-version && bun run version:sync",
    "bump:major": "bun pm version major --no-git-tag-version && bun run version:sync",
    "tag": "git tag -a \"v$(bun run --silent version:print)\" -m \"v$(bun run --silent version:print)\"",
    "tag:push": "git push && git push origin \"v$(bun run --silent version:print)\"",
    "release": "bun run version:sync && git add -A && git commit -m \"chore: release v$(bun run --silent version:print)\" && bun run tag && bun run tag:push",
    "vscode:package": "cd vscode-extension && bun run package",
    "vscode:release": "bun run version:sync && bun run vscode:package"
  },
  "dependencies": {
    "@jxpeng98/markdown-it-citation": "1.2.1",
    "@slidev/client": "^52.9.1",
    "@slidev/types": "^52.9.1"
  },
  "devDependencies": {
    "@slidev/cli": "^52.9.1",
    "vitepress": "2.0.0-alpha.12"
  },
  "//": "Learn more: https://sli.dev/guide/write-theme.html",
  "slidev": {
    "colorSchema": "both",
    "defaults": {
      "aspectRatio": "4:3",
      "fonts": {
        "sans": "Inter",
        "serif": "Merriweather",
        "mono": "JetBrains Mono"
      }
    },
    "themeConfig": {
      "author": "Your Name",
      "conference": "Your Conference"
    }
  }
}
